# ===============================================
# LOCKED MODE - Input Blocking (RED)
# ===============================================
# Activated with: Ctrl+g l
# Exit with: Ctrl+g (unlock)

# Status should reflect locked mode
set -g status-right '#[fg=#1F1F28,bg=#E46A78,bold] LOCKED #[fg=#DDD8BB,bg=#1F1F28] %H:%M #[fg=#363644]#[fg=#DDD8BB,bg=#363644] #[fg=#7EB3C9,bg=#363644]#[fg=#1F1F28,bg=#7EB3C9,bold] %d %b '

# Unbind ALL root bindings - complete input lockdown
unbind -T root -a

# Prevent input while in mode
select-pane -d
set-hook -g client-attached "select-pane -d"
set-hook -g client-focus-in "select-pane -d"
set-hook -g window-pane-changed "select-pane -d"
set-hook -g session-window-changed "select-pane -d"

# ONLY allow Ctrl+g to unlock and return to normal mode
bind -T root C-g run-shell "~/.config/tmux/scripts/return_to_normal.sh" \; set -g status-right "#[fg=#363644]#[fg=#DDD8BB,bg=#363644] %H:%M #[fg=#7EB3C9,bg=#363644]#[fg=#1F1F28,bg=#7EB3C9,bold] %d %b " \; unbind -T root -a \; bind -n M-H previous-window \; bind -n M-L next-window \; bind -n M-[ previous-layout \; bind -n M-] next-layout
